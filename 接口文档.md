# 接口文档

与 [`设计文档.md`](设计文档.md) 配套；Web 管理端通过 `{base}/ai_repo_scan_service` 调用后端 REST 服务。

**服务前缀**：`{base}/ai_repo_scan_service`  
示例：`http://reposcan-beta.rnd.huawei.com/repo-scan-service/ai_repo_scan_service`

**统一响应**：`{ meta, data }`。成功时 `meta.isSuccess = true`；失败时 `meta.isSuccess = false`，`data` 通常为 `null`。

**实现状态说明**：

| 标记 | 含义 |
|------|------|
| 已实现 | 当前 Web 管理端 `taskManagementApi` 已对接或 Mock 已对齐 |
| 规划中 | 设计文档已定义，后端/前端尚未落地 |

---

## 接口索引

| 编号 | 方法 | 路径 | 说明 | 状态 |
|------|------|------|------|------|
| 1.1 | GET | `/api/tasks` | 查询任务列表 | 已实现 |
| 1.2 | GET | `/api/tasks/{taskId}/info` | 查询任务基本信息 | 已实现 |
| 1.3 | GET | `/api/tasks/{taskId}/scan-results` | 分页查询扫描结果 | 已实现 |
| 1.4 | POST | `/api/tasks` | 创建任务 | 已实现 |
| 1.5 | PUT | `/api/tasks/{taskId}` | 更新任务信息 | 已实现 |
| 1.6 | DELETE | `/api/tasks/{taskId}` | 删除任务 | 已实现 |
| 1.7 | POST | `/api/tasks/{taskId}/start` | 启动扫描 | 已实现 |
| 1.8 | POST | `/api/tasks/{taskId}/uploadDataSet` | 上传扫描结果文件 | 已实现 |
| 1.9 | GET | `/api/tasks/{taskId}/annotation-statistics` | 标注与规则统计 | 已实现 |
| 2.1 | POST | `/api/annotations` | 保存单条标注 | 已实现 |
| 3.1 | POST | `/api/annotations/review` | 保存评审结果 | 规划中 |
| 3.2 | GET | `/api/tasks/{taskId}/annotations/{warnUuid}/submit-history` | 标注提交历史 | 规划中 |
| 3.3 | GET | `/api/tasks/{taskId}/annotations/{warnUuid}/review-history` | 评审历史 | 规划中 |
| 3.4 | GET | `/api/tasks/{taskId}/annotations/{warnUuid}/timeline` | 操作时间线 | 规划中 |

---

# 一、数据定义

## 0、统一响应元信息（ApiResponseMeta）

| 字段 | 类型 | 说明 |
|------|------|------|
| number | number | HTTP 状态码或业务码，如 200、400 |
| message | string | 提示信息 |
| isSuccess | boolean | 是否成功 |

## 1、任务列表项（TaskListApiRow）

`GET /api/tasks` 响应 `data.list` 单条。

| 字段 | 类型 | 说明 |
|------|------|------|
| taskId | string | 任务 ID，以 T 开头的 UUID |
| taskName | string | 任务名称 |
| taskStatus | string | 未开始 / 排队中 / 进行中 / 已完成 / 失败 |
| assistantVersions | string | 扫描助手版本，逗号分隔，如 `v1.0.0` |
| branch | string | 扫描分支 |
| codeLanguage | string \| null | 代码语言 |
| createTime | string | 创建时间，yyyy-mm-dd hh:mm:ss |
| creator | string | 创建人短工号 |
| lineNum | number \| null | 扫描代码量（k） |
| pathList | string | 扫描路径，逗号分隔 |
| productName | string | 产品名称 |
| repoUrl | string | 代码仓 URL |
| s3Path | string | 扫描结果 S3 路径 |
| warnCount | number \| null | 告警总数 |
| deptName | string \| null | 部门 |
| pduName | string \| null | PDU |
| scanResults | array | 列表接口固定 `[]` |
| paginationInfo | null | 列表接口固定 `null` |

## 2、任务详情基本信息（TaskInfo）

`GET /api/tasks/{taskId}/info` 响应 `data`。

| 字段 | 类型 | 说明 |
|------|------|------|
| taskId | string | 任务 ID |
| taskName | string | 任务名称 |
| repoUrl | string | 代码仓 URL |
| branch | string | 分支 |
| pathList | string | 扫描路径 |
| assistantVersions | string[] | 助手版本数组 |
| creator | string | 创建人短工号 |
| createTime | string | 创建时间 |
| taskStatus | string | 任务状态 |
| codeLanguage | string | 代码语言 |
| lineNum | number | 代码量（k） |
| productName | string | 产品名称 |
| s3Path | string | S3 路径 |
| warnCount | number | 告警总数 |
| hostUrl | string | 扫描 Agent URL（可选） |
| modelName | string | 扫描模型名称（可选） |
| deptName | string \| null | 部门 |
| pduName | string \| null | PDU |
| scanResults | null | 本接口不返回扫描明细 |
| paginationInfo | null | 本接口不返回分页 |

## 3、分页信息（PaginationInfo）

扫描结果分页，用于 `scan-results` 响应 `data.paginationInfo`。

| 字段 | 类型 | 说明 |
|------|------|------|
| totalPages | number | 总页数 |
| pageSize | number | 每页条数 |
| hasPrevious | boolean | 是否有上一页 |
| hasNext | boolean | 是否有下一页 |
| currentPage | number | 当前页码 |
| totalCount | number | 总记录数 |

## 4、扫描结果项（ScanResultItem）

| 字段 | 类型 | 说明 |
|------|------|------|
| warn_uuid | string | 告警 UUID |
| warn_line | number | 告警行号 |
| warn | string | 告警描述 |
| warn_code_block | string | 告警代码块 |
| check_function_id | string \| null | 函数 ID |
| code_snippet | string | 代码片段 |
| confidence | number | 置信度 |
| context | string | 上下文代码 |
| start_line | number | 起始行 |
| end_line | number | 结束行 |
| file_name | string | 文件名 |
| function_name | string | 函数名 |
| func_uuid | string | 函数 UUID |
| self_increment_id | number | 序号 |
| index | number \| null | 索引 |
| reason | string | 行内原因（通常为空，以 annotation 为准） |
| rule_name | string | 规则名称 |
| annotation | Annotation \| null | 标注信息 |

## 5、标注信息（Annotation）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | number | 标注记录 ID |
| warnUuid | string | 告警 UUID |
| userId | string | 标注人短工号 |
| issueResult | number | 0-需要修改，1-无需修改，2-问题误报 |
| reason | string \| null | 标注原因 |
| annotationStatus | number | 1-已标注 |
| createTime | string | 创建时间 |
| updateTime | string | 更新时间 |
| userName | string \| null | 标注人姓名 |
| userDepartment | string \| null | 标注人部门 |
| taskId | string \| null | 任务 ID |
| reviewStatus | number \| null | **规划中**：0-待评审，1-已通过 |
| reviewerUserId | string \| null | **规划中**：评审人短工号 |
| reviewTime | string \| null | **规划中**：评审时间 |

## 6、用户信息（UserInfo）

| 字段 | 类型 | 说明 |
|------|------|------|
| w3Id | string | 短工号 |
| name | string | 长工号 |
| nameCn | string | 中文姓名 |

## 7、标注提交历史（AnnotationSubmitHistory）— 规划中

| 字段 | 类型 | 说明 |
|------|------|------|
| id | number | 主键 |
| taskId | string | 任务 ID |
| warnUuid | string | 告警 UUID |
| annotationId | number \| null | 关联标注主表 |
| action | string | submit / update / resubmit / cancel |
| roundNo | number | 标注轮次 |
| userId | string | 标注人短工号 |
| userName | string \| null | 标注人姓名 |
| userDepartment | string \| null | 部门 |
| issueResult | number \| null | 提交时结论 |
| reason | string \| null | 提交时原因 |
| submitTime | string | 提交时间 |

## 8、标注评审历史（AnnotationReviewHistory）— 规划中

| 字段 | 类型 | 说明 |
|------|------|------|
| id | number | 主键 |
| taskId | string | 任务 ID |
| warnUuid | string | 告警 UUID |
| submitHistoryId | number | 关联提交历史 ID |
| annotationId | number \| null | 冗余标注主表 ID |
| action | string | approve / reject |
| reviewerUserId | string | 评审人短工号 |
| reviewerUserName | string \| null | 评审人姓名 |
| reviewTime | string | 评审时间 |
| rejectReason | string \| null | 驳回理由 |
| submitSnapshot | AnnotationSubmitHistory \| null | 可选展开被评审标注 |

## 9、操作时间线事件（TimelineEvent）— 规划中

| 字段 | 类型 | 说明 |
|------|------|------|
| type | string | submit / review |
| time | string | 事件时间 |
| action | string | 同对应历史表 action |
| issueResult | number \| null | 标注结论 |
| reason | string \| null | 标注原因 |
| userId | string \| null | 标注人 |
| userName | string \| null | 标注人姓名 |
| roundNo | number \| null | 轮次 |
| reviewerUserId | string \| null | 评审人 |
| reviewerUserName | string \| null | 评审人姓名 |
| rejectReason | string \| null | 驳回理由 |
| submitHistoryId | number \| null | 提交历史 ID |

---

# 二、接口实现

## 1、任务管理

### 1.1 查询任务列表（带过滤）

**请求方式**：`GET /api/tasks`

**Query 参数**：

| 参数 | 必填 | 说明 |
|------|------|------|
| pageNum | 是 | 页码 |
| pageSize | 是 | 每页条数 |
| creator | 否 | 按创建人短工号筛选 |
| taskStatus | 否 | 任务状态 |
| taskName | 否 | 任务名称模糊搜索 |

**响应示例**：

```json
{
  "meta": {
    "number": 200,
    "message": "OK",
    "isSuccess": true
  },
  "data": {
    "total": 9,
    "pages": 2,
    "size": 8,
    "page": 2,
    "list": [
      {
        "taskId": "Tba19e29a-52c2-404f-aaba-c9e70ca3bcd3",
        "taskName": "5GCore_CGF",
        "repoUrl": "https://codehub-y.huawei.com/5gcore/cp/domain/CGF/files?ref=master",
        "branch": "master",
        "pathList": "product/src/module",
        "s3Path": "RepoScan/5GCore_CGF/aiMemorySafeCheckResult.json",
        "creator": "t00598420",
        "createTime": "2026-03-13 14:58:45",
        "taskStatus": "已完成",
        "assistantVersions": "v1.0.0",
        "productName": "NCG",
        "codeLanguage": null,
        "lineNum": null,
        "deptName": null,
        "pduName": null,
        "warnCount": 1651,
        "scanResults": [],
        "paginationInfo": null
      }
    ]
  }
}
```

### 1.2 查询任务基本信息

**请求方式**：`GET /api/tasks/{taskId}/info`

**路径参数**：`taskId`

**响应示例**：

```json
{
  "meta": {
    "isSuccess": true,
    "message": "OK",
    "number": 200
  },
  "data": {
    "taskId": "T00112233-4455-6677-8899-aabbccddeeff",
    "taskName": "前端代码扫描任务",
    "repoUrl": "https://github.com/example/frontend.git",
    "branch": "main",
    "pathList": "src,main",
    "assistantVersions": ["v2.0.0", "v2.1.0"],
    "creator": "a00559876",
    "createTime": "2024-01-15 10:30:00",
    "taskStatus": "已完成",
    "codeLanguage": "JavaScript",
    "lineNum": 1.5,
    "productName": "UDM",
    "s3Path": "RepoScan/frontend/result.json",
    "warnCount": 1577,
    "hostUrl": "http://localhost:8080",
    "modelName": "gpt-4",
    "deptName": null,
    "pduName": null,
    "scanResults": null,
    "paginationInfo": null
  }
}
```

### 1.3 查询扫描结果（分页）

**请求方式**：`GET /api/tasks/{taskId}/scan-results`

**Query 参数**：

| 参数 | 必填 | 说明 |
|------|------|------|
| pageNum | 是 | 页码 |
| pageSize | 是 | 每页条数 |
| ruleName | 否 | 规则名称筛选 |
| annotation | 否 | 标注筛选：`unmarked` / `0` / `1` / `2` |
| reviewStatus | 否 | **规划中**：`0` 待评审 / `1` 已通过 |

**响应示例**：

```json
{
  "meta": {
    "isSuccess": true,
    "message": "OK",
    "number": 200
  },
  "data": {
    "scanResults": [
      {
        "file_name": "sdc/domain/libcompiler/adr_compile.c",
        "function_name": "ASN1_AddTagAndLen",
        "start_line": 43,
        "end_line": 72,
        "code_snippet": "",
        "context": "",
        "func_uuid": "5f10f739-1927-4a93-bf6b-cbec62c0061e",
        "self_increment_id": 1,
        "check_function_id": null,
        "index": null,
        "rule_name": "内存安全",
        "warn_line": 51,
        "warn_code_block": ";",
        "warn": "内存分配后未检查大小是否足够。",
        "reason": "",
        "confidence": 0,
        "warn_uuid": "5427cb40-aa79-4f99-aabd-f77da06222a9",
        "annotation": {
          "id": 16,
          "warnUuid": "5427cb40-aa79-4f99-aabd-f77da06222a9",
          "userId": "t00598420",
          "issueResult": 1,
          "reason": null,
          "annotationStatus": 1,
          "createTime": "2026-03-10 10:01:41",
          "updateTime": "2026-03-10 10:01:41",
          "userName": null,
          "userDepartment": null,
          "taskId": "Tba19e29a-52c2-404f-aaba-c9e70ca3bcd3"
        }
      }
    ],
    "paginationInfo": {
      "totalPages": 158,
      "pageSize": 10,
      "hasPrevious": false,
      "hasNext": true,
      "currentPage": 1,
      "totalCount": 1577
    }
  }
}
```

### 1.4 创建任务

**请求方式**：`POST /api/tasks`

**请求体**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| taskName | string | 是 | 任务名称 |
| productName | string | 是 | 产品名称 |
| repoUrl | string | 否 | 代码仓 URL |
| branch | string | 否 | 分支 |
| pathList | string | 否 | 扫描路径，逗号分隔 |
| creator | string | 否 | 创建人短工号 |
| assistantVersions | string | 否 | 助手版本，逗号分隔 |
| codeLanguage | string | 否 | 代码语言 |
| deptName | string | 否 | 部门 |
| pduName | string | 否 | PDU |
| lineNum | number | 否 | 代码量（k） |

**响应示例（成功）**：

```json
{
  "meta": {
    "isSuccess": true,
    "message": "OK",
    "number": 200
  },
  "data": {
    "taskId": "T00b6b10f-e768-4c72-bd6f-9a087f78c847",
    "taskName": "新建任务",
    "productName": "CDN",
    "taskStatus": "未开始",
    "createTime": "2026-02-27 10:00:00"
  }
}
```

**失败示例**：

```json
{
  "meta": {
    "isSuccess": false,
    "message": "任务名称不能为空",
    "number": 400
  },
  "data": null
}
```

### 1.4.1 批量创建任务

**请求方式**：`POST /api/tasks/batch`

**说明**：一次性创建多条代码仓扫描任务。字段与「1.4 创建任务」一致，并额外支持在创建时写入 `hostUrl`（本机启动 URL）、`modelName`（模型名称）。创建人 `creator`、`nameCn` 在请求体顶层统一指定，各任务行无需重复填写。

**请求体**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| creator | string | 是 | 创建人短工号 |
| nameCn | string | 否 | 创建人中文名 |
| tasks | array | 是 | 任务列表，单次最多 100 条 |

**tasks[] 单条字段**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| taskName | string | 是 | 任务名称 |
| productName | string | 是 | 产品名称 |
| repoUrl | string | 是 | 代码仓 HTTPS Git 克隆地址 |
| branch | string | 是 | 扫描分支 |
| pathList | string | 否 | 扫描路径，逗号分隔 |
| assistantVersions | string | 否 | 助手版本，逗号分隔；默认「内存安全v1.0.0」 |
| codeLanguage | string | 否 | 代码语言；默认「C/C++」 |
| deptName | string | 否 | 部门 |
| pduName | string | 否 | PDU |
| lineNum | number | 否 | 代码量（k） |
| hostUrl | string | 否 | 本机启动 URL；与 modelName 须同时填写或同时留空 |
| modelName | string | 否 | 模型名称；与 hostUrl 须同时填写或同时留空 |

**请求示例**：

```json
{
  "creator": "t00598420",
  "nameCn": "张三",
  "tasks": [
    {
      "taskName": "批量任务-01",
      "productName": "CDN",
      "repoUrl": "https://codehub.example.com/org/demo.git",
      "branch": "main",
      "pathList": "src,lib",
      "assistantVersions": "内存安全v1.0.0",
      "codeLanguage": "C/C++",
      "lineNum": 100,
      "deptName": "研发部",
      "pduName": "PDU-A",
      "hostUrl": "http://localhost:8080",
      "modelName": "gpt-4"
    }
  ]
}
```

**响应示例（部分成功）**：

```json
{
  "meta": {
    "isSuccess": true,
    "message": "OK",
    "number": 200
  },
  "data": {
    "totalCount": 2,
    "successCount": 1,
    "failureCount": 1,
    "results": [
      {
        "rowIndex": 1,
        "success": true,
        "taskId": "T00b6b10f-e768-4c72-bd6f-9a087f78c847",
        "taskName": "批量任务-01"
      },
      {
        "rowIndex": 2,
        "success": false,
        "taskName": "批量任务-02",
        "message": "代码仓Git地址格式无效"
      }
    ]
  }
}
```

**失败示例（整批无效）**：

```json
{
  "meta": {
    "isSuccess": false,
    "message": "任务列表不能为空",
    "number": 400
  },
  "data": null
}
```

### 1.5 更新任务信息

**请求方式**：`PUT /api/tasks/{taskId}`

**路径参数**：`taskId`

**请求体**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| taskName | string | 是 | 任务名称 |
| repoUrl | string | 是 | 代码仓 URL |
| branch | string | 是 | 分支 |
| pathList | string \| null | 否 | 扫描路径 |
| s3Path | string | 是 | S3 路径 |
| taskStatus | string | 是 | 任务状态 |
| assistantVersions | string | 是 | 助手版本 |
| productName | string | 是 | 产品名称 |
| codeLanguage | string \| null | 否 | 代码语言 |
| lineNum | number \| null | 否 | 代码量 |
| deptName | string \| null | 否 | 部门 |
| pduName | string \| null | 否 | PDU |
| warnCount | number \| null | 否 | 告警总数 |
| hostUrl | string \| null | 否 | Agent URL（启动扫描前必填） |
| modelName | string \| null | 否 | 模型名称（启动扫描前必填） |

**请求示例**：

```json
{
  "taskName": "验证规则解析",
  "repoUrl": "https://codehub-y.huawei.com/example/repo/files?ref=master",
  "branch": "main",
  "pathList": null,
  "s3Path": "RepoScan/验证规则解析/aiMemorySafeCheckResult.json",
  "taskStatus": "已完成",
  "assistantVersions": "v1.0.0",
  "productName": "UDM",
  "codeLanguage": null,
  "lineNum": null,
  "deptName": null,
  "pduName": null,
  "warnCount": 36,
  "hostUrl": "http://localhost:8080",
  "modelName": "gpt-4"
}
```

**响应示例**：

```json
{
  "meta": {
    "number": 200,
    "message": "OK",
    "isSuccess": true
  },
  "data": null
}
```

### 1.6 删除任务

**请求方式**：`DELETE /api/tasks/{taskId}`

**路径参数**：`taskId`

**响应示例（成功）**：

```json
{
  "meta": {
    "isSuccess": true,
    "message": "success",
    "number": 200
  },
  "data": "任务删除成功"
}
```

**注意事项**：

- 排队中、进行中的任务不可删除（见设计文档业务规则）。
- 删除时联动清理任务关联数据与缓存。

### 1.7 启动扫描

**请求方式**：`POST /api/tasks/{taskId}/start`

**路径参数**：`taskId`

**请求体**：空对象 `{}`

**前置条件**：任务已配置非空 `hostUrl`、`modelName`；任务状态为未开始、已完成或失败；排队中/进行中不可重复启动。

**响应示例（成功）**：

```json
{
  "meta": {
    "isSuccess": true,
    "message": "OK",
    "number": 200
  },
  "data": {
    "message": "扫描任务已启动",
    "taskId": "Tba19e29a-52c2-404f-aaba-c9e70ca3bcd3",
    "taskStatus": "排队中"
  }
}
```

**失败示例**：

```json
{
  "meta": {
    "isSuccess": false,
    "message": "请先填写并保存本机启动URL与模型名称",
    "number": 400
  },
  "data": null
}
```

### 1.8 上传扫描结果文件

**请求方式**：`POST /api/tasks/{taskId}/uploadDataSet`

**Content-Type**：`multipart/form-data`

**表单字段**：

| 字段 | 必填 | 说明 |
|------|------|------|
| file | 是 | 扫描结果 JSON 文件 |
| userId | 是 | 上传人短工号 |

**响应示例**：

```json
{
  "data": {
    "meta": {
      "isSuccess": true,
      "message": "上传成功",
      "number": 200
    },
    "data": "AIRepoScan/task_123456/scan_result.json"
  }
}
```

> 注意：上传接口顶层仅含 `data`，其内再嵌套 `meta` 与路径字符串，与其他接口略有不同。

### 1.9 获取标注与规则统计

**请求方式**：`GET /api/tasks/{taskId}/annotation-statistics`

**响应示例**：

```json
{
  "meta": {
    "number": 200,
    "message": "OK",
    "isSuccess": true
  },
  "data": {
    "taskId": "T3296229f-3c51-4a75-bcc4-3dd20031948e",
    "taskName": "验证规则解析",
    "totalWarnCount": 36,
    "annotatedCount": 8,
    "unannotatedCount": 28,
    "annotationCompletionRate": 22.22,
    "statusDistribution": [
      {
        "statusCode": 1,
        "statusDescription": "已标注",
        "warnCount": 8,
        "percentage": 100.0
      }
    ],
    "annotationDistribution": [
      {
        "resultCode": 2,
        "resultDescription": "需要修改",
        "annotationCount": 2,
        "percentage": 25.0
      },
      {
        "resultCode": 1,
        "resultDescription": "无需修改的问题",
        "annotationCount": 5,
        "percentage": 62.5
      },
      {
        "resultCode": 0,
        "resultDescription": "非问题",
        "annotationCount": 1,
        "percentage": 12.5
      }
    ],
    "ruleStatistics": [
      {
        "taskId": "T3296229f-3c51-4a75-bcc4-3dd20031948e",
        "ruleName": "内存安全",
        "ruleCount": 36,
        "updateTime": "2026-03-25 16:50:27"
      }
    ]
  }
}
```

**字段说明**：

- `annotationDistribution`：`resultCode` 与 `issueResult` **不是同一套编码**，映射见附录 A。
- `pendingReviewCount`、`approvedReviewCount`、`rejectedReviewCount`：**规划中**，见第 3 章统计扩展。

---

## 2、缺陷标注

### 2.1 保存单个缺陷标注

**请求方式**：`POST /api/annotations`

**请求体**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| taskId | string | 是 | 任务 ID |
| warnUuid | string | 是 | 告警 UUID |
| issueResult | number \| null | 是 | 0/1/2；`null` 表示取消标注 |
| userId | string | 是 | 标注人短工号 |
| userName | string | 否 | 标注人姓名 |
| reason | string | 否 | 标注原因 |

**请求示例**：

```json
{
  "taskId": "Tba19e29a-52c2-404f-aaba-c9e70ca3bcd3",
  "warnUuid": "6bc8163a-3d2d-4d58-a70b-f07775295c1a",
  "issueResult": 0,
  "userId": "t00598420",
  "reason": "默认规则",
  "userName": "田园"
}
```

**响应示例（成功）**：

```json
{
  "meta": {
    "number": 200,
    "message": "标注更新成功",
    "isSuccess": true
  },
  "data": {
    "id": 19,
    "warnUuid": "6bc8163a-3d2d-4d58-a70b-f07775295c1a",
    "userId": "t00598420",
    "issueResult": 0,
    "reason": "默认规则",
    "annotationStatus": 1,
    "createTime": "2026-03-23 10:45:59",
    "updateTime": "2026-03-23 10:45:59",
    "userName": "田园",
    "userDepartment": null,
    "taskId": "Tba19e29a-52c2-404f-aaba-c9e70ca3bcd3"
  }
}
```

**失败 - 无权限（HTTP 403）**：

```json
{
  "meta": {
    "number": 403,
    "message": "仅首次标注人可修改该告警标注",
    "isSuccess": false
  },
  "data": null
}
```

**业务规则**：

- 未标注时任何人可首次提交；已标注仅首次标注人可修改或取消。
- **规划中扩展**：保存时同步写入 `t_annotation_submit_history`，响应增加 `reviewStatus=0`、`submitHistoryId`。

---

## 3、标注评审（规划中）

> 本章接口已在 [`设计文档.md`](设计文档.md) 3.7 节定义，Web 端尚未对接；后端落地后以本章契约为准。

### 3.1 保存缺陷标注评审结果

**请求方式**：`POST /api/annotations/review`

**请求体**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| taskId | string | 是 | 任务 ID |
| warnUuid | string | 是 | 告警 UUID |
| decision | string | 是 | `approve` 通过 / `reject` 驳回 |
| comment | string | 驳回必填 | 评审意见；驳回时为驳回理由 |

**请求示例 - 通过**：

```json
{
  "taskId": "Tba19e29a-52c2-404f-aaba-c9e70ca3bcd3",
  "warnUuid": "6bc8163a-3d2d-4d58-a70b-f07775295c1a",
  "decision": "approve",
  "comment": "结论合理"
}
```

**请求示例 - 驳回**：

```json
{
  "taskId": "Tba19e29a-52c2-404f-aaba-c9e70ca3bcd3",
  "warnUuid": "6bc8163a-3d2d-4d58-a70b-f07775295c1a",
  "decision": "reject",
  "comment": "标注结论与代码实际情况不符，请重新核对"
}
```

**响应 - 通过**：

```json
{
  "meta": {
    "number": 200,
    "message": "评审通过",
    "isSuccess": true
  },
  "data": {
    "annotation": {
      "id": 19,
      "warnUuid": "6bc8163a-3d2d-4d58-a70b-f07775295c1a",
      "issueResult": 0,
      "reviewStatus": 1,
      "reviewerUserId": "r00123456",
      "reviewTime": "2026-03-28 14:30:00"
    },
    "reviewHistoryId": 201,
    "submitHistoryId": 101
  }
}
```

**响应 - 驳回**：

```json
{
  "meta": {
    "number": 200,
    "message": "评审已驳回",
    "isSuccess": true
  },
  "data": {
    "annotation": null,
    "reviewHistoryId": 202,
    "submitHistoryId": 101
  }
}
```

### 3.2 查询标注提交历史

**请求方式**：`GET /api/tasks/{taskId}/annotations/{warnUuid}/submit-history`

返回 `data.list`：`AnnotationSubmitHistory[]`，按 `submitTime` 降序。

### 3.3 查询标注评审历史

**请求方式**：`GET /api/tasks/{taskId}/annotations/{warnUuid}/review-history`

返回 `data.list`：`AnnotationReviewHistory[]`，按 `reviewTime` 降序；可 JOIN 展开 `submitSnapshot`。

### 3.4 查询操作时间线

**请求方式**：`GET /api/tasks/{taskId}/annotations/{warnUuid}/timeline`

**响应示例**：

```json
{
  "meta": {
    "number": 200,
    "message": "OK",
    "isSuccess": true
  },
  "data": {
    "taskId": "Tba19e29a-52c2-404f-aaba-c9e70ca3bcd3",
    "warnUuid": "6bc8163a-3d2d-4d58-a70b-f07775295c1a",
    "events": [
      {
        "type": "submit",
        "time": "2026-03-23 10:45:59",
        "action": "submit",
        "issueResult": 0,
        "reason": "默认规则",
        "userId": "t00598420",
        "roundNo": 1,
        "submitHistoryId": 101
      },
      {
        "type": "review",
        "time": "2026-03-28 14:00:00",
        "action": "reject",
        "reviewerUserId": "r00123456",
        "rejectReason": "结论不对",
        "submitHistoryId": 101
      },
      {
        "type": "submit",
        "time": "2026-03-28 15:00:00",
        "action": "resubmit",
        "issueResult": 1,
        "reason": "经复核为误报",
        "roundNo": 2,
        "submitHistoryId": 102
      },
      {
        "type": "review",
        "time": "2026-03-28 16:00:00",
        "action": "approve",
        "reviewerUserId": "r00123456",
        "submitHistoryId": 102
      }
    ]
  }
}
```

**统计接口扩展（1.9 响应 `data` 新增字段）**：

| 字段 | 说明 |
|------|------|
| pendingReviewCount | 待评审条数 |
| approvedReviewCount | 已通过条数 |
| rejectedReviewCount | 累计驳回次数 |

---

# 附录

## A. annotationDistribution.resultCode 与 issueResult 映射

`annotationDistribution` 用于统计看板，其 `resultCode` 与标注接口 `issueResult` 编码不同：

| resultCode | resultDescription | 对应 issueResult |
|------------|-------------------|------------------|
| 2 | 需要修改 | 0 |
| 1 | 无需修改的问题 | 1 |
| 0 | 非问题 | 2（问题误报） |

## B. issueResult 枚举

| issueResult | 含义 |
|-------------|------|
| null / 未写入 | 未标注 |
| 0 | 需要修改 |
| 1 | 无需修改 |
| 2 | 问题误报 |

## C. reviewStatus 枚举（规划中）

| reviewStatus | 含义 |
|--------------|------|
| null | 未标注或驳回后已重置 |
| 0 | 待评审 |
| 1 | 已通过 |

## D. 任务状态枚举

| taskStatus | 含义 |
|------------|------|
| 未开始 | 可编辑、可启动 |
| 排队中 | 已提交启动，不可重复启动、不可删除 |
| 进行中 | Agent 执行中，不可删除 |
| 已完成 | 可查询结果与标注 |
| 失败 | 可修正后重启 |

---

*文档版本：与 Web 管理端 `taskManagementApi` 及设计文档 3.3 节对齐；评审相关接口标记为规划中。*
