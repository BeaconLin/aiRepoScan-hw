# v0608 — 专家评审后端开发指导

**版本**：2026-06-08  
**适用范围**：`ai_repo_scan_service` 后端（PostgreSQL schema `ai_code_repo_scan`）  
**关联文档**：[`设计文档.md`](设计文档.md) §3.7、[`专家评审方案.md`](专家评审方案.md)、[`接口文档.md`](接口文档.md)  
**前端状态**：Web 管理端（本仓库）已完成 Mock 与 UI 对接，待后端联调

---

## 1. 背景与目标

标注员提交单条告警标注后，条目进入 **未评审** 状态；专家执行 **通过 / 驳回** 后更新主表评审字段。**仅「已通过」（`review_status=1`）视为业务终态**。

本次采用 **轻量化主表扩展方案**（v0608 定稿）：

- 评审当前态 **仅存于** `t_repo_scan_annotation`
- **不新建** `t_annotation_submit_history`、`t_annotation_review_history`、`task_reviewer`
- 驳回后 **保留** 标注员的 `issue_result`、`reason`，不再清空主表回到「未标注」

---

## 2. 接口变更清单

### 2.1 新增接口

| 方法 | 路径 | 说明 |
|------|------|------|
| **POST** | `/api/annotations/review` | 专家对单条标注执行通过或驳回 |

### 2.2 修改接口

| 方法 | 路径 | 变更摘要 |
|------|------|----------|
| **POST** | `/api/annotations` | 响应增加评审字段；保存/修改时 `review_status→0`；取消标注时删除记录并清评审 |
| **GET** | `/api/tasks/{taskId}/scan-results` | Query 增加 `reviewStatus`；`annotation` 增加评审相关字段 |
| **GET** | `/api/tasks/{taskId}/annotation-statistics` | 响应增加 `pendingReviewCount` / `approvedReviewCount` / `rejectedReviewCount` |

### 2.3 本版本 **不实现** 的接口

以下接口在旧版 `接口文档.md` 第 3 章中标记为「规划中」，**v0608 方案已废弃**，后端无需开发：

| 方法 | 路径 | 原因 |
|------|------|------|
| GET | `/api/tasks/{taskId}/annotations/{warnUuid}/submit-history` | 无独立提交历史表 |
| GET | `/api/tasks/{taskId}/annotations/{warnUuid}/review-history` | 无独立评审历史表 |
| GET | `/api/tasks/{taskId}/annotations/{warnUuid}/timeline` | 无时间线聚合需求 |

> 联调时前端主要依赖 scan-results 内嵌 `annotation` 字段展示评审态，不调用上述历史接口。

---

## 3. 数据库设计

### 3.1 表：`t_repo_scan_annotation`（扩展）

Schema：`ai_code_repo_scan`

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `review_status` | int2 | NOT NULL, DEFAULT 0 | 0 未评审 / 1 已通过 / 2 已驳回 |
| `reviewer_user_id` | varchar(50) | NULL | 最近评审人短工号 |
| `review_time` | timestamp(6) | NULL | 最近评审时间 |
| `review_comment` | text | NULL | 评审意见（驳回时业务必填） |
| `final_issue_result` | int2 | NULL | 可选：专家确认后的最终结论 0/1/2 |

已有字段（保持不变）：`id`, `warn_uuid`, `task_id`, `user_id`, `user_name`, `user_department`, `issue_result`, `reason`, `annotation_status`, `create_time`, `update_time`。

**索引建议**：

```sql
CREATE INDEX IF NOT EXISTS idx_annotation_task_review_status
  ON ai_code_repo_scan.t_repo_scan_annotation (task_id, review_status);
```

**唯一约束**：`(task_id, warn_uuid)` 保持不变。

### 3.2 DDL 迁移脚本（示例）

> 若生产库已有 `review_status` 但无 `review_comment` / `final_issue_result`，按需执行 ALTER；若字段均已存在则跳过。

```sql
-- 1. 新增评审扩展字段（不存在时）
ALTER TABLE ai_code_repo_scan.t_repo_scan_annotation
  ADD COLUMN IF NOT EXISTS review_comment text,
  ADD COLUMN IF NOT EXISTS final_issue_result int2;

-- 2. 确保 review_status 非空且默认 0
ALTER TABLE ai_code_repo_scan.t_repo_scan_annotation
  ALTER COLUMN review_status SET DEFAULT 0;

UPDATE ai_code_repo_scan.t_repo_scan_annotation
  SET review_status = 0
  WHERE review_status IS NULL;

ALTER TABLE ai_code_repo_scan.t_repo_scan_annotation
  ALTER COLUMN review_status SET NOT NULL;

-- 3. 索引
CREATE INDEX IF NOT EXISTS idx_annotation_task_review_status
  ON ai_code_repo_scan.t_repo_scan_annotation (task_id, review_status);

-- 4. 历史数据：已有标注且无评审记录的，统一视为「未评审」
UPDATE ai_code_repo_scan.t_repo_scan_annotation
  SET review_status = 0
  WHERE annotation_status = 1
    AND review_status IS DISTINCT FROM 1
    AND review_status IS DISTINCT FROM 2;
```

### 3.3 各场景数据库操作

| 场景 | 操作 | 字段变更 |
|------|------|----------|
| 首次标注 | INSERT | 写入标注字段；`review_status=0`；评审字段为空 |
| 修改标注 | UPDATE | 更新 `issue_result`/`reason`；`review_status=0` |
| 驳回后重提 | UPDATE | 更新标注内容；`review_status=0`；清空 `reviewer_user_id`, `review_time`, `review_comment` |
| 取消标注 | DELETE | `issueResult=null` 时删除 `(task_id, warn_uuid)` 对应行 |
| 评审通过 | UPDATE | `review_status=1`；写 reviewer、time、comment（可选）、final_issue_result（可选） |
| 评审驳回 | UPDATE | `review_status=2`；写 reviewer、time、comment（必填）；**保留** issue_result、reason |
| 列表筛选 | SELECT | JOIN 标注表后按 `review_status` 过滤 |
| 评审统计 | SELECT COUNT | 按 `task_id` + `review_status` 聚合（仅已标注：`issue_result IS NOT NULL`） |

---

## 4. 接口契约（与前端 Mock 对齐）

**服务前缀**：`{base}/ai_repo_scan_service`  
**命名**：DB `snake_case` ↔ API `camelCase`

### 4.1 POST `/api/annotations`（修改）

#### 请求体（不变）

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| taskId | string | 是 | 任务 ID |
| warnUuid | string | 是 | 告警 UUID |
| issueResult | number \| null | 是 | 0/1/2；**null 表示取消标注** |
| userId | string | 是 | 标注人短工号 |
| userName | string | 否 | 标注人姓名 |
| reason | string | 否 | 标注原因 |

#### 响应 `data` 扩展字段

| 字段 | 类型 | 说明 |
|------|------|------|
| reviewStatus | number | 保存成功后固定为 **0** |
| reviewerUserId | string \| null | 新保存时为 null |
| reviewerUserName | string \| null | 新保存时为 null |
| reviewTime | string \| null | 新保存时为 null |
| reviewComment | string \| null | 新保存时为 null |
| finalIssueResult | number \| null | 新保存时为 null |

#### 业务规则

1. **未标注**：任何人可首次 INSERT；`review_status=0`。
2. **已标注**：仅 **首次标注人**（`user_id` 匹配）可 UPDATE 或 DELETE；否则 **403**。
3. **保存/修改**：`review_status=0`；若原状态为 **已驳回(2)**，清空 `reviewer_user_id`、`review_time`、`review_comment`。
4. **取消**（`issueResult=null`）：DELETE 该行；前端会刷新列表，该条恢复为未标注。

---

### 4.2 GET `/api/tasks/{taskId}/scan-results`（修改）

#### 新增 Query 参数

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| reviewStatus | string | 否 | `0` 未评审 / `1` 已通过 / `2` 已驳回 |

与现有 `pageNum`、`pageSize`、`ruleName`、`annotation` 并列。

#### `annotation` 对象扩展

| 字段 | 类型 | 说明 |
|------|------|------|
| reviewStatus | number | 0 / 1 / 2 |
| reviewerUserId | string \| null | 评审人短工号 |
| reviewerUserName | string \| null | 评审人姓名（建议从 IAM/用户表解析，Mock 有返回） |
| reviewTime | string \| null | yyyy-MM-dd HH:mm:ss |
| reviewComment | string \| null | 评审意见 |
| finalIssueResult | number \| null | 专家最终结论（可选） |

#### 筛选逻辑

- `reviewStatus=0`：已标注且 `review_status=0`
- `reviewStatus=1`：已标注且 `review_status=1`
- `reviewStatus=2`：已标注且 `review_status=2`
- 未传：不过滤

---

### 4.3 GET `/api/tasks/{taskId}/annotation-statistics`（修改）

在现有 `annotationCompletionRate`、`annotationDistribution`、`ruleStatistics` 等字段基础上 **新增**：

| 字段 | 类型 | 统计口径 |
|------|------|----------|
| pendingReviewCount | number | 已标注（`issue_result` 非空）且 `review_status=0` |
| approvedReviewCount | number | `review_status=1` |
| rejectedReviewCount | number | `review_status=2` |

**注意**：评审统计与「标注完成度」分离——完成度仍按是否存在有效标注计算；评审统计仅针对已标注条目。

---

### 4.4 POST `/api/annotations/review`（新增）

#### 请求体

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| taskId | string | 是 | 任务 ID |
| warnUuid | string | 是 | 告警 UUID |
| decision | string | 是 | `approve` 或 `reject` |
| comment | string | 驳回必填 | 评审意见 |
| finalIssueResult | number | 否 | 0/1/2；仅 `approve` 且产品允许专家改结论时传入 |

#### 权限

- 调用者须具备 IAM 角色 **「专家」**（前端 Mock：`roles` 含 `expert`）；否则 **403**。
- 评审人从登录态 / 网关注入的用户信息获取，**不要**信任客户端伪造的 reviewer 字段。

#### 前置校验

| 条件 | 错误码 | message 示例 |
|------|--------|--------------|
| 标注不存在或未标注 | 400 | 当前告警不可评审，仅未评审状态可执行评审操作 |
| `review_status != 0` | 400 | 当前告警不可评审，仅未评审状态可执行评审操作 |
| `decision=reject` 且 comment 为空 | 400 | 驳回时必须填写评审意见 |
| 非专家角色 | 403 | 无评审权限 |

#### 通过（approve）— UPDATE

```sql
UPDATE t_repo_scan_annotation
SET review_status = 1,
    reviewer_user_id = :reviewerUserId,
    review_time = NOW(),
    review_comment = :comment,          -- 可选
    final_issue_result = :finalIssueResult,  -- 可选
    update_time = NOW()
WHERE task_id = :taskId AND warn_uuid = :warnUuid AND review_status = 0;
-- affected_rows = 0 时返回 409（并发冲突）
```

#### 驳回（reject）— UPDATE

```sql
UPDATE t_repo_scan_annotation
SET review_status = 2,
    reviewer_user_id = :reviewerUserId,
    review_time = NOW(),
    review_comment = :comment,          -- 必填
    update_time = NOW()
    -- issue_result、reason 不变
WHERE task_id = :taskId AND warn_uuid = :warnUuid AND review_status = 0;
```

#### 响应 `data`（与前端 Mock 对齐）

```json
{
  "annotation": { /* 完整 Annotation 对象，含 reviewStatus 等 */ },
  "reviewHistoryId": 0,
  "submitHistoryId": 0,
  "reviewRecord": {
    "action": "approve",
    "reviewerUserId": "t00598420",
    "reviewerUserName": "田园",
    "reviewTime": "2026-06-08 10:00:00",
    "rejectReason": null
  },
  "rejectedAnnotation": null
}
```

> **v0608 变更**：驳回时 `annotation` **不为 null**，`reviewStatus=2`，保留原标注内容；`reviewHistoryId`/`submitHistoryId` 固定返回 `0`（无历史表）。`reviewRecord.rejectReason` 在驳回时填 comment。

---

## 5. 状态机

```
[未标注]
    │ POST /api/annotations (issueResult=0/1/2)
    ▼
[已标注 + 未评审]  review_status=0
    │
    ├─ POST /review approve ──► [已标注 + 已通过]  review_status=1  （终态）
    │
    └─ POST /review reject  ──► [已标注 + 已驳回]  review_status=2
                                      │
                                      │ POST /api/annotations（修改后保存）
                                      ▼
                              [已标注 + 未评审]  review_status=0

[已标注] ── POST /api/annotations (issueResult=null) ──► [未标注]（DELETE 主表行）
```

---

## 6. 后端开发步骤（推荐顺序）

### 阶段 0：准备（0.5 天）

- [ ] 确认 PostgreSQL 连接与 schema `ai_code_repo_scan` 可写
- [ ] 执行 §3.2 DDL 迁移
- [ ] 确认 IAM / 网关如何注入当前用户及 `roles`（至少能识别「专家」）
- [ ] 本地启动后端，前端 `VITE_API_REPO_SCAN` 指向本地（如 `http://localhost:8662`）

### 阶段 1：实体与 Mapper（1 天）

- [ ] Entity/DTO 增加：`reviewStatus`, `reviewerUserId`, `reviewTime`, `reviewComment`, `finalIssueResult`
- [ ] `reviewerUserName` 可在 Service 层通过用户服务解析，不必落库
- [ ] AnnotationMapper：`selectByTaskAndWarnUuid`、`updateReview`、`deleteByTaskAndWarnUuid`
- [ ] 保存标注 Mapper：INSERT/UPDATE 时写 `review_status=0`；驳回重提时清空 reviewer 字段

### 阶段 2：改造已有三个接口（1～2 天）

**优先级：标注保存 → 扫描结果 → 统计**

1. **POST `/api/annotations`**
   - 响应补齐评审字段
   - 取消标注改为 DELETE
   - 驳回后重提清空 reviewer 字段

2. **GET `scan-results`**
   - 解析 `reviewStatus` query，在 JOIN 标注表后过滤
   - `annotation` 序列化增加 6 个评审字段

3. **GET `annotation-statistics`**
   - 增加三条 COUNT SQL（或一次 GROUP BY）

### 阶段 3：新增评审接口（1 天）

- [ ] `ReviewController` + `ReviewService`
- [ ] 专家角色校验（403）
- [ ] approve / reject 分支 + 乐观锁（`WHERE review_status=0`）
- [ ] 组装响应 `SaveAnnotationReviewResultData`

### 阶段 4：联调与测试（1 天）

- [ ] 前端 `apiMode` 切 `live`，走真实后端
- [ ] 按 §7 测试用例逐项验证
- [ ] 修复 camelCase / 时间格式不一致问题

---

## 7. 测试用例清单

| # | 场景 | 步骤 | 期望 |
|---|------|------|------|
| 1 | 首次标注 | POST annotations issueResult=0 | reviewStatus=0，DB review_status=0 |
| 2 | 专家通过 | POST review approve | reviewStatus=1，reviewer 有值 |
| 3 | 专家驳回 | POST review reject + comment | reviewStatus=2，issue_result 保留 |
| 4 | 驳回后修改 | POST annotations 改 reason | reviewStatus=0，reviewer 字段清空 |
| 5 | 取消标注 | POST annotations issueResult=null | 主表行删除，scan-results 无 annotation |
| 6 | 重复评审 | 已通过再 review | 400 |
| 7 | 非专家评审 | 无 expert 角色 | 403 |
| 8 | 驳回无意见 | reject 无 comment | 400 |
| 9 | 筛选未评审 | scan-results?reviewStatus=0 | 仅未评审已标注条目 |
| 10 | 统计 | GET annotation-statistics | 三个 reviewCount 与 DB 一致 |
| 11 | 他人改标注 | 非首次标注人 POST | 403 |
| 12 | 并发评审 | 两人同时 approve 同一 warn | 一人 200，一人 409 |

---

## 8. 与前端联调要点

| 项目 | 说明 |
|------|------|
| 服务地址 | 前端默认 `http://localhost:8662/ai_repo_scan_service` |
| 评审入口 | 任务详情 → 扫描结果列表 → 行内「通过」「驳回」（需 expert 角色） |
| 筛选 | 列表区「评审状态筛选」传 `reviewStatus` |
| 统计卡片 | 「专家评审进度」读 `pendingReviewCount` 等三个字段 |
| 取消标注 | 「取消标注」按钮 → `issueResult=null`，需 DELETE 主表 |
| 时间格式 | 建议 `yyyy-MM-dd HH:mm:ss`，与现有 `createTime` 一致 |
| 响应结构 | 统一 `{ meta: { isSuccess, number, message }, data }` |

前端关键类型文件（联调字段对照）：

- `src/api/types/saveAnnotation.ts` — `ReviewStatus`, `SaveAnnotationResultData`
- `src/api/types/annotationReview.ts` — `SaveAnnotationReviewReqBody`
- `src/api/types/taskApiDoc.ts` — `TaskScanResultAnnotationApiDoc`

---

## 9. 常见问题

**Q：旧版「驳回后清空标注」的数据怎么办？**  
A：v0608 不再清空。若历史库已按旧逻辑清空，无法自动恢复；新驳回一律 `review_status=2` 且保留标注。

**Q：还需要 submit-history / review-history 接口吗？**  
A：不需要。若 `接口文档.md` 仍保留第 3.2～3.4 节，后续可单独修订文档标记为「已废弃」。

**Q：`finalIssueResult` 是否必须实现？**  
A：字段预留即可。当前前端「通过」未传该字段；后端收到则写入，未收到则 NULL，以 `issue_result` 为准。

**Q：评审人姓名从哪来？**  
A：建议从 IAM / 用户服务按 `reviewer_user_id` 解析；scan-results 与 review 响应中的 `reviewerUserName` 供前端展示。

---

## 10. 修订记录

| 日期 | 说明 |
|------|------|
| 2026-06-08 | 初版：基于 git diff 与轻量化评审方案整理，供后端开发使用 |

---

*本文档为 v0608 迭代的后端实施单页指南；接口细节以实现联调结果为准，重大变更请同步更新 [`接口文档.md`](接口文档.md)。*
